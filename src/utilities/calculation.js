const calculation = (first, second) => {
  return first + second;
};

const multiply = (first, second) => {
  return first * second;
};

const calculateTheFirstNumberDivideByTheSecondNumber = (first, second) => {
  return first / second;
};

// export default calculation;
export {
  multiply,
  calculation,
  calculateTheFirstNumberDivideByTheSecondNumber,
};
